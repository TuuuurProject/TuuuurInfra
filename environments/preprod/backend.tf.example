# Exemple de backend GCS (RECOMMANDÉ en prod)
# 1) créez un bucket GCS dédié au state, avec IAM strict.
# 2) copiez ce fichier en backend.tf puis `terraform init -reconfigure`

terraform {
  backend "gcs" {
    bucket = "CHANGE_ME_TFSTATE_BUCKET"
    prefix = "webplat/preprod"
  }
}
